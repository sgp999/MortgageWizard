# MortgageWizard

A new Python project scaffolded for MortgageWizard.

## Get Started

Install in editable mode:

```bash
pip install -e .
```

Run the package:

```bash
python -m mortagewizard
```

## Structure

- `src/mortagewizard/` — package code
- `tests/` — unit tests
